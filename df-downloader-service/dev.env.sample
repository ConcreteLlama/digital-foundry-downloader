# This is more of a live config dir and is currently only used for the ignore list
CONFIG_DIR=./config

# Dir for db.json
DB_DIR=./db

# Optional. Overrides config/jwt-secret.yaml, which is otherwise generated on
# first run. Changing it invalidates every issued login token.
# JWT_SIGNING_SECRET=